<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="meetboard">
		
		<!-- selectList -->
		<select id="meetBoardSelectList" parameterType="int" resultType="meetDto">
			SELECT MEET_NO, MEET_REGDATE, MEET_ADDR, MEET_TITLE, MEET_WRITER, VOTE_STARTDATE, VOTE_ENDDATE
			FROM MEET_BOARD
			WHERE S_NO = #{s_no}
			ORDER BY MEET_NO DESC  
		</select> 
		
		<select id="totalBoardNum" parameterType="int"	resultType="_int">
			SELECT COUNT(*)
			FROM FREE_BOARD
			WHERE S_NO = #{s_no}
		</select>
		
		<!-- paging -->
		<select id="pagingMeetBoardList" parameterType="hashMap" resultMap="boardMap">
			<![CDATA[
				SELECT B_NO, S_NO, B_TITLE, B_WRITER, B_CONTENT, B_REGDATE, B_CNT
				FROM
				(
					SELECT ROWNUM N, B_NO, S_NO, B_TITLE, B_WRITER, B_CONTENT, B_REGDATE, B_CNT
					FROM FREE_BOARD
					WHERE S_NO = #{studyno}
					ORDER BY B_NO DESC
				)
				WHERE N >= #{startRow} AND N <= #{endRow}
			]]>
		</select>
	
	
	
	</mapper>